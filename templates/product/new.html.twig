{% extends 'base.html.twig' %}

{% block title %}New Product{% endblock %}

{% block body %}
    <div class="container"><br>
        <h1>Create un nouveau produit</h1><br>

        {{ include('product/_form.html.twig', {'button_label': 'Add'}) }}

        {# {{ include('product/_form.html.twig') }} #}

        <a class="btn btn-outline-primary" href="{{ path('app_product_index') }}">Retour à la liste</a>
    </div>
    {# Ce n'est pas un formulaire que je dois rendre mais juste une liste pour selectionner les tailles #}
{% endblock %}
