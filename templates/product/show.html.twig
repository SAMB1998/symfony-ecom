{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <div class="container"><br>
        <h1>Product</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ product.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ product.name }}</td>
                </tr>
                <tr>
                    <th>Size</th>
                    <td>{{ product.size }}</td>
                </tr>
                <tr>
                    <th>Price</th>
                    <td>{{ product.price }}</td>
                </tr>
            </tbody>
        </table>

        <a class="btn btn-outline-primary" href="{{ path('app_product_index') }}">Retour à la liste</a>

        <a class="btn btn-outline-primary" href="{{ path('app_product_edit', {'id': product.id}) }}">Modifier</a>

        <a class="btn btn-secondary" href="{{ path('app_product_stock_add_history', {'id': product.id}) }}">afficher l'historique du sctock</a>
        {{ include('product/_delete_form.html.twig') }}
    </div>
{% endblock %}
